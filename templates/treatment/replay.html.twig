{% extends 'base.html.twig' %}

{% block title %}Correction des données{% endblock %}

{% block body %}
<div class="container">
    <h1 class="my-4">Correction des données</h1>

    {# Affichage des messages d'erreur (flash) #}
    {% for message in app.flashes('error') %}
        <div class="error-message mt-4 alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}

    {# Affichage des messages de succès (flash) #}
    {% for message in app.flashes('success') %}
        <div class="success-message mt-4 alert alert-success">
            {{ message }}
        </div>
    {% endfor %}

    <!-- Tableau des données -->
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    {% for title in selected_column_titles %}
                        <th>{{ title|default('') }}</th>  {# Assurez-vous d'afficher un en-tête vide si nécessaire #}
                    {% endfor %}
                </tr>
            </thead>
            <tbody>
                {% for rowIndex in filtered_data %}
                    <tr>
                        {% for cellIndex in selected_columns %}
                            <td>{{ rowIndex[cellIndex] | default('') }}</td>
                        {% endfor %}
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <!-- Bouton de retour -->
    <form action="{{ path('app_home') }}" method="get" class="mt-4">
        <div class="fw-semibold fs-6 text-gray-500 mb-7">
            Une fois les modifications finies, ainsi que le nouveau fichier téléchargé
            <br>vous pouvez retourner à la page d'accueil et soumettre un nouveau fichier
        </div>
        <button class="btn btn-light-warning" type="submit">Retour à l'Accueil</button>
    </form>
</div>
{% endblock %}
